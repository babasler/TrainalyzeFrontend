import 'package:flutter/material.dart';

class NewPlan extends StatelessWidget {
  const NewPlan({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("➕ Neuer Trainingsplan"));
  }
}
