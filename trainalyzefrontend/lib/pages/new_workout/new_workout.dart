import 'package:flutter/material.dart';

class NewWorkout extends StatelessWidget {
  const NewWorkout({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("➕ Neues Workout"));
  }
}
